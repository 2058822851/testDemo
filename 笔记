1、安装git客户端
    git bash 支持Linux命名的git控制台（常用）
    git CMD 支持windows命令的控制台
    git GUI 可视化界面
2、VSCode 安装git插件
    快捷键：Ctrl + ~
3、gitHub官网网站，注册账号


1、打开git bash
    输入命令切换到当前目录
2、配置git基本操作
    git config --global user.name "2058822851"(添加注册GitHub时的昵称)
    git config --global user.name "2058822851@qq.com"

    打开官网，点击加号NEw repository

    继续
3、 git init初始化  生成.git文件
4、工作区 =》暂存区
    git add 文件名
    git add *提交所有文件

    git commit -m "这次提交的描述"
5、查看当前工作区的状态 git status
6、git checkout 从暂存区见文件恢复提交到工作区
7、git diff 查看工作区和暂存区的区别
8、git log 查看已经提交的历史版本
9、git reset --hard HEAD^ 退回到上一版本
10、git reset --hard 版本号 退回到指定版本

11、从暂存区提交到远程仓库，生死ssh密匙
    ssh-keygen -t rsa -C "你的GitHub邮箱"
    找到.pub文件
12、配置密匙
13、暂存区=》 原创仓库
    1、git remote add origin "https://github.com/2058822851/testDemo.git"(仓库地址)
 